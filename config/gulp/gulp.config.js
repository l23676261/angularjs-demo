const config = {
    /*
    * 是否开启动态注册任务模块
    * */
    dynamic: false,
    /*
    * 执行 全部编译 （npm run dev） 模块
    * */
    compile: [
        'drug',
        'pharmacy'
    ]
};
module.exports = config;
